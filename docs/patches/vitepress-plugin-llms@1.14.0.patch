diff --git a/dist/index.js b/dist/index.js
index 36612eeae11b6a232e52548fb020eeacea3858c0..fc51d7fca47e7c55488ab5a8795aef2d9ebacf16 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -930,9 +930,11 @@ function extractImageMap(bundle) {
 	}
 	return imageMap;
 }
+// facioquo patch: POSIX paths on Windows. The directory-index rule below must
+// match markdownPath() in docs/.vitepress/routes.ts.
 function resolvePagePath(file, workDir, config) {
-	const resolved = path.relative(workDir, resolveOutputFilePath(file, workDir, config.vitepress.userConfig.rewrites));
-	const directory = path.dirname(resolved);
+	const resolved = transformToPosixPath(path.relative(workDir, resolveOutputFilePath(file, workDir, config.vitepress.userConfig.rewrites)));
+	const directory = path.posix.dirname(resolved);
 	return path.basename(resolved) === "index.md" && directory !== "." && directory !== "" ? `${directory}.md` : resolved;
 }
 async function prepareMarkdownFiles({ bundle, mdFiles, settings, config }) {
diff --git a/dist/vitepress-components/CopyOrDownloadAsMarkdownButtons.vue b/dist/vitepress-components/CopyOrDownloadAsMarkdownButtons.vue
index 5cbd240c46cbe7873b27b6912040e22ac952ff8e..1503a5b8b3715f85dcd97630b29a6b61d5fa4fd7 100644
--- a/dist/vitepress-components/CopyOrDownloadAsMarkdownButtons.vue
+++ b/dist/vitepress-components/CopyOrDownloadAsMarkdownButtons.vue
@@ -19,7 +19,12 @@
 					<span class="divider"></span>
 
 					<!-- Chevron area -->
-					<button class="chevron-wrapper" @click.stop="toggleDropdown">
+					<button
+						class="chevron-wrapper"
+						aria-label="More page actions"
+						:aria-expanded="isOpen"
+						@click.stop="toggleDropdown"
+					>
 						<span v-html="iconChevron" class="icon chevron" :class="{ open: isOpen }"></span>
 					</button>
 				</div>
@@ -32,6 +37,11 @@
 						<span v-html="iconExternal" class="icon external"></span>
 					</button>
 
+					<button class="dropdown-item" @click="handleDownloadMarkdown">
+						<span v-html="downloaded ? iconCheck : iconDownload" class="icon"></span>
+						{{ theme.llms?.downloadText || 'Download as Markdown' }}
+					</button>
+
 					<button
 						v-for="provider in aiProviders"
 						:key="provider.name"
@@ -49,11 +59,6 @@
 					</button>
 				</div>
 			</div>
-
-			<!-- Download button -->
-			<button class="download-btn" @click="downloadMarkdown">
-				<span v-html="downloaded ? iconCheck : iconDownload" class="icon"></span>
-			</button>
 		</div>
 	</div>
 </template>
@@ -136,6 +141,11 @@ function handleViewAsMarkdown(): void {
 	closeDropdown()
 }
 
+function handleDownloadMarkdown(): void {
+	downloadMarkdown()
+	closeDropdown()
+}
+
 function handleOpenInAI(provider: MarkdownAiProvider): void {
 	openInAI(provider)
 	closeDropdown()
@@ -161,7 +171,6 @@ onUnmounted(() => document.removeEventListener('click', handleClickOutside))
 .markdown-copy-buttons-inner {
 	margin: 16px 0;
 	display: flex;
-	gap: 8px;
 	position: relative;
 }
 
@@ -257,17 +266,6 @@ onUnmounted(() => document.removeEventListener('click', handleClickOutside))
 	opacity: 0.6;
 }
 
-.download-btn {
-	display: flex;
-	align-items: center;
-	padding: 8px 12px;
-	background: transparent;
-	border: 1px solid var(--vp-c-divider);
-	border-radius: 6px;
-	color: var(--vp-c-text-1);
-	cursor: pointer;
-}
-
 .icon {
 	width: 18px;
 	height: 18px;
@@ -292,8 +290,7 @@ onUnmounted(() => document.removeEventListener('click', handleClickOutside))
 
 	/* Hover zones */
 	.copy-page:hover,
-	.chevron-wrapper:hover,
-	.download-btn:hover {
+	.chevron-wrapper:hover {
 		background: var(--vp-c-bg-soft);
 	}
 
@@ -301,13 +298,11 @@ onUnmounted(() => document.removeEventListener('click', handleClickOutside))
 	.copy-page,
 	.chevron-wrapper,
 	.dropdown-item,
-	.dropdown-item .icon.external,
-	.download-btn {
+	.dropdown-item .icon.external {
 		transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
 	}
 
-	.dropdown-trigger:hover,
-	.download-btn:hover {
+	.dropdown-trigger:hover {
 		border-color: var(--vp-c-brand-1);
 		transform: translateY(-1px);
 		box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
